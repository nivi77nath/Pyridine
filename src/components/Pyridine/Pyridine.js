import React from "react";
import PropTypes from "prop-types";
import "./Pyridine.scss";
//import { Test } from './Pyridine.styles';

const Pyridine = (props) => (
  <div className="PyridineWrapper">
    <h1>Pyridine</h1>
  </div>
);

Pyridine.propTypes = {
  // bla: PropTypes.string,
};

Pyridine.defaultProps = {
  // bla: 'test',
};

export default Pyridine;
